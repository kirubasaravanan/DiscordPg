# Copy this file to .env and fill in real values for your machine.
# .env is gitignored — never commit real credentials.

DATABASE_URL=postgresql+psycopg://pgos:changeme@localhost:5432/pgos_dev
DATABASE_ECHO=false
